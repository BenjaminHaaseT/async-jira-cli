//! A module to provide thread safe and an asynchronous interface for the database

use std::sync::Arc;
use crate::models::prelude::*;

/// A thread safe asynchronous interface for interacting with the database.
pub struct DbInterface {

}

